package org.dromara.common.core.enums;

/**
 * Account entry calculated from enabled roles rather than from user_type.
 */
public enum EntryType {

    /** Administrative application entry. */
    ADMIN,

    /** Learner application entry. */
    LEARNING
}
